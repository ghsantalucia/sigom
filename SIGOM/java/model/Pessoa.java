package model;

public class Pessoa {

	private int id;
	/*
		ocorrencia (int)
		tipo_pessoa (string)
		nome (string)
		data_nascimento (string)
		observacao (text)
		endereco (text)
		PESSOA (ocorrencia) REFERENCES OCORRENCIA (ID)
	 */
}
