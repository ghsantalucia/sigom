package model;

public class Lesao {

	private int id;
	/*
		vitima (int)
		dia_lesoes (int)
		tipo_lesao (string)
		causa (string)
		localizacao_lesao (string)
		descricao (text)
		quantidade (int)
		num_lesao (int)
		arma_usada (string)
		observacao (text)
		LESAO (vitima) REFERENCES VITIMA (ID)
		LESAO (dia_lesoes) REFERENCES DIA_LESOES (ID)
	 */
}
