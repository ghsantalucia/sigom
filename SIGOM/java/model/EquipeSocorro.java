package model;

public class EquipeSocorro {

	private int id;
	/*
	 	ocorrencia (int)
		viatura (string)
		medico_crm (string)
		instituicao (string)
		horario_obito (string)
		data_obito (string)
		observacao (text)
		EQUIPE_SOCORRO (ocorrencia) REFERENCES OCORRENCIA (ID)
	 */
	
}
