package model;

public class Isolamento {

	private int id;
	/*
		ocorrencia (int)
		instituicao (string)
		viatura (string)
		batalhao (string)
		responsaveis (string)
		preservacao_contento (boolean)
		isolamento_contento (boolean)
		observacao_preservacao (text)
		observacao_isolamento (text)
		observacao_geral (text)
		ISOLAMENTO (ocorrencia) REFERENCES OCORRENCIA (ID)
	 */
}
